# Modern CLI Tools

Prefer modern, faster alternatives:

- Use `rg` (ripgrep) instead of `grep` - faster, respects .gitignore
- Use `fd` instead of `find` - faster, simpler syntax, respects .gitignore
